{
  "name": "vb-code-vault",
  "version": "1.0.0",
  "private": true,
  "description": "Static Grimoire tools. Unit tests for the Anmerkung rule engine run via the built-in Node test runner (no build step).",
  "scripts": {
    "test": "node --test",
    "verify:anmerkung": "node scripts/anmerkung/verify.mjs",
    "test:anmerkung": "node --test tests/processors.test.mjs tests/diff.test.mjs tests/anmerkung-v5.test.mjs",
    "dev": "node scripts/dev-server.mjs"
  }
}
